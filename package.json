{
  "name": "hellenic-geo-society",
  "version": "1.0.0",
  "description": "Hellenic Geographical Society Website",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:preview": "vitepress preview docs",
    "docs:serve": "vitepress serve docs",
    "docs:build": "vitepress build docs",
    "docs:deploy": "vitepress build docs && cd docs/.vitepress/dist && git init && git add -A && git commit -m 'deploy' && git push -f https://github.com/HGS-web/HGS.git main:public"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitepress": "^1.5.0",
    "vue": "^3.4.19"
  }
}
